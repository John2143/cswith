Windows and mabye mac
1. Install [node](https://nodejs.org/)
2. Click start.bat (should not need admin privelages)
Not Windows
1. sudo apt-get install nodejs
2. chmod +x start.sh
3. ./start.sh


When in game (in console)
	clear;status
	condump
And then open up
	./site.html
